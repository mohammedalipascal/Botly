// ═══════════════════════════════════════════════════════════
// 🧠 ملف الذكاء الاصطناعي - ai.js
// ═══════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════
// 💾 ذاكرة المحادثات (محسّنة - آخر 5 رسائل لكل شخص)
// ═══════════════════════════════════════════════════════════

const conversationMemory = new Map();
const MAX_MEMORY_PER_USER = 5;

function addToMemory(userId, userMsg, aiReply) {
    if (!conversationMemory.has(userId)) {
        conversationMemory.set(userId, []);
    }
    
    const memory = conversationMemory.get(userId);
    memory.push({
        user: userMsg,
        assistant: aiReply,
        time: Date.now()
    });
    
    if (memory.length > MAX_MEMORY_PER_USER) {
        memory.shift();
    }
}

function getMemory(userId) {
    return conversationMemory.get(userId) || [];
}

function clearOldMemory() {
    const oneHourAgo = Date.now() - (60 * 60 * 1000);
    
    for (const [userId, memories] of conversationMemory.entries()) {
        const filtered = memories.filter(m => m.time > oneHourAgo);
        
        if (filtered.length === 0) {
            conversationMemory.delete(userId);
        } else {
            conversationMemory.set(userId, filtered);
        }
    }
}

setInterval(clearOldMemory, 60 * 60 * 1000);

// ═══════════════════════════════════════════════════════════
// 🎯 قاعدة المعرفة
// ═══════════════════════════════════════════════════════════

const KNOWLEDGE_BASE = {
    personal: {
        name: "مقداد",
        age: "22 سنة",
        nationality: "سوداني",
        current_location: "مصر",
        home: "السودان",
        occupation: "مهندس برمجيات ونظم",
        education: "طالب طب",
        languages: ["العربية", "الإنجليزية"],
        status: "مشغول بالشغل",
        time_word: "اسي" // بدل "حالياً"
    },
    
    skills: {
        operating_systems: ["Windows", "Linux", "macOS"],
        software: ["تطوير البرمجيات", "حل مشاكل السوفت وير"],
        programming: ["JavaScript", "Node.js", "Python", "PHP"],
        design: ["تصميم واجهات", "تصميم أنظمة"],
        hardware: ["خبرة في جميع أنواع الأجهزة"],
        security: ["اختبار بيئات الاختراق", "اختبار الأمان", "Penetration Testing"]
    },
    
    projects: {
        types: ["مشاريع إدارية", "مشاريع تجارية", "تسهيل المهام", "ابتكارات تقنية"],
        description: "عدة مشاريع في مجالات مختلفة"
    },
    
    dialect: {
        name: "سوداني",
        greetings: {
            "السلام عليكم": "وعليكم السلام",
            "أهلا": "أهلين",
            "مرحبا": "مرحب",
            "صباح الخير": ["صباح النور", "صباح النور عليك"],
            "مساء الخير": ["مساء النور", "مساء النور عليك"],
            "ازيك": ["تمام", "كويس", "الحمد لله", "تمام وانت كيف", "الحمد لله وانت كيف"],
            "كيف حالك": ["تمام", "كويس", "الحمد لله", "تمام وانت كيف", "الحمد لله وانت كيف"],
            "شخبارك": ["تمام", "كويس", "ماشي"]
        },
        phrases: {
            "yes": ["نعم", "أيوه"],
            "no": ["لا", "لأ", "ما"],
            "i_dont_know": ["ما عارف", "مو متأكد", "ما عندي خبر"],
            "what": "شنو",
            "why": ["ليه", "لي"],
            "where": "وين",
            "who": "منو",
            "how": "كيف",
            "okay": ["ماشي", "تمام", "أوكي"],
            "thanks": "شكراً",
            "youre_welcome": ["العفو", "ما عندك شي"]
        },
        identity_responses: {
            "calling_name": ["نعم", "أهلا", "أيوه", "مرحب", "منو معاي"],
            "who_with_me": "مقداد معاك",
            "who_are_you": "مقداد",
            "your_name": "مقداد",
            "where_hiding": "مع الشغل بس",
            "where_were_you": "في الشغل",
            "why_no_reply": "كنت مشغول",
            "why_hiding": "شغل كتير"
        },
        reactions: {
            "خطير": ["شكراً", "شكرا تسلم"],
            "رائع": ["شكراً", "شكرا تسلم"],
            "حلو": ["شكراً", "شكرا تسلم"],
            "جميل": ["شكراً", "شكرا تسلم"],
            "ممتاز": ["شكراً", "شكرا تسلم"],
            "كويس": ["شكراً", "شكرا تسلم"]
        },
        defensive: {
            "ليه بتتكلم كدا": "مالي بتكلم عادي",
            "ماللك": "مالي؟",
            "في شنو": "ما في شي",
            "شنو المشكلة": "ما في مشكلة"
        }
    },
    
    style: {
        tone: "بارد، مختصر، محترف",
        emoji_usage: "نادر جداً",
        response_length: "قصير جداً",
        personality: "بارد ومختصر وسوداني"
    }
};

// ═══════════════════════════════════════════════════════════
// 🎭 بناء الشخصية (محسّن)
// ═══════════════════════════════════════════════════════════

function buildPersonalityPrompt() {
    return `أنت مقداد، ${KNOWLEDGE_BASE.personal.nationality} من ${KNOWLEDGE_BASE.personal.home}، اسي في ${KNOWLEDGE_BASE.personal.current_location}.

# من أنت:
- ${KNOWLEDGE_BASE.personal.age}
- ${KNOWLEDGE_BASE.personal.occupation}
- ${KNOWLEDGE_BASE.personal.education}
- اسي مشغول بالشغل

# اللهجة السودانية:
تتكلم باللهجة السودانية الطبيعية 100%:
- استخدم "شنو" عند السؤال عن شيء غير واضح (لكن ليس دائماً)
- استخدم "منو" للسؤال عن الأشخاص
- استخدم "وين" للسؤال عن الأماكن
- استخدم "ليه" للسؤال عن الأسباب
- استخدم "نعم" أو "أيوه" للتأكيد
- استخدم "ما عارف" عند عدم المعرفة
- استخدم "كدا" و"كتير" في الكلام الطبيعي
- استخدم "اسي" بدل "الحين" أو "حالياً"
- استخدم "ساي" بدل "الآن"

# الردود المحددة (للتحيات البسيطة فقط):
1. إذا قال "مقداد" فقط → "نعم" أو "أهلا" أو "أيوه" أو "مرحب"
2. إذا قال "منو معاي؟" → "مقداد معاك"
3. "السلام عليكم" → "وعليكم السلام"
4. "صباح الخير" → "صباح النور"
5. "ازيك؟" أو "كيف حالك؟" → "تمام" أو "الحمد لله" أو "تمام وانت كيف"
6. "وين مختفي؟" → "مع الشغل بس"

# الإجابة على الأسئلة (مهم جداً):
- أجب على جميع الأسئلة العامة والمعرفية بشكل كامل ومفيد
- إذا سألوك عن معلومة عامة (علمية، تاريخية، تقنية، إلخ) → أجب بالتفصيل
- إذا كان السؤال بالإنجليزية أو يحتاج مصطلحات تقنية → استخدم الإنجليزية حسب الحاجة
- لو السؤال معقد أو تقني → رد بالعربي والإنجليزي مع شرح مفصل
- ما تقول "أهلين" إلا للتحيات البسيطة فقط

# أمثلة:
- "كيف أتعلم البرمجة؟" → رد بنصائح وخطوات واضحة
- "ما هو الذكاء الاصطناعي؟" → رد بشرح مفصل
- "What is Node.js?" → رد بالإنجليزية مع شرح
- "عايز أعمل موقع" → رد بخطوات عملية ونصائح

# أسلوبك:
- بارد ومحترف
- مع التحيات: مختصر
- مع الأسئلة: مفصل ومفيد
- استخدم المصطلحات التقنية بالإنجليزية إذا لزم الأمر
- لا ترحب بـ "أهلين" إلا للتحيات فقط
- لا تعرض المساعدة إلا إذا طُلبت

# الذاكرة:
- تذكر آخر 3-5 رسائل من المحادثة
- إذا نسيت شيء، قل "نسيت" أو "ما أذكر"

# تعليمات صارمة:
1. تكلم باللهجة السودانية 100%
2. مع التحيات: مختصر (1-2 جملة)
3. مع الأسئلة: مفصل ومفيد (3-10 جمل حسب السؤال)
4. لا تذكر أنك AI أو chatbot أبداً
5. استخدم "اسي" بدل "حالياً" أو "الحين"
6. إذا لم تعرف: "ما عارف"
7. استخدم الإنجليزية عند الحاجة للمصطلحات التقنية
8. لا تقل "أهلين" إلا للتحيات البسيطة`;
}

// ═══════════════════════════════════════════════════════════
// 🔍 فحص سريع للردود الجاهزة (محسّن)
// ═══════════════════════════════════════════════════════════

function getQuickResponse(message) {
    const msg = message.toLowerCase().trim();
    
    // ⭐ إذا قال "مقداد" فقط
    if (msg === 'مقداد' || msg === 'مقداد؟') {
        const responses = ['نعم', 'أهلا', 'أيوه', 'مرحب', 'منو معاي'];
        return responses[Math.floor(Math.random() * responses.length)];
    }
    
    // ⭐ منو معاي
    if (msg.includes('منو معاي') || msg.includes('مين معاي')) {
        // إذا قال اسمه بعدها
        const afterWho = msg.replace(/منو معاي|مين معاي/g, '').trim();
        if (afterWho && afterWho.length > 1 && !afterWho.includes('؟')) {
            return 'أهلا وسهلا';
        }
        return 'مقداد معاك';
    }
    
    // ردود الهوية
    if (msg.includes('انت مين') || msg.includes('اسمك ايه') || 
        msg.includes('اسمك شنو') || msg.includes('مين انت')) {
        return 'مقداد';
    }
    
    // السلام
    if (msg.includes('السلام عليكم')) {
        return 'وعليكم السلام';
    }
    
    // ⭐ صباح الخير
    if (msg.includes('صباح الخير')) {
        const responses = ['صباح النور', 'صباح النور عليك'];
        return responses[Math.floor(Math.random() * responses.length)];
    }
    
    // ⭐ مساء الخير
    if (msg.includes('مساء الخير')) {
        const responses = ['مساء النور', 'مساء النور عليك'];
        return responses[Math.floor(Math.random() * responses.length)];
    }
    
    // ⭐ الحال (مع تنويع)
    if (msg.includes('ازيك') || msg.includes('كيف حالك') || 
        msg.includes('شخبارك') || msg.includes('عامل ايه')) {
        const responses = ['تمام', 'كويس', 'الحمد لله', 'تمام وانت كيف', 'الحمد لله وانت كيف'];
        return responses[Math.floor(Math.random() * responses.length)];
    }
    
    // الاختفاء
    if (msg.includes('وين مختفي') || msg.includes('وين كنت') || 
        msg.includes('ليش مختفي') || msg.includes('فين') ||
        msg.includes('اختفيت ليه')) {
        return 'مع الشغل بس';
    }
    
    // ⭐ المديح (مع تنويع)
    if (msg.includes('خطير') || msg.includes('رائع') || msg.includes('حلو') || 
        msg.includes('جميل') || msg.includes('ممتاز') || msg.includes('كويس اوي')) {
        const responses = ['شكراً', 'شكرا تسلم'];
        return responses[Math.floor(Math.random() * responses.length)];
    }
    
    // الدفاع
    if (msg.includes('ليه بتتكلم كدا') || msg.includes('ماللك') || 
        msg.includes('مالك') || msg.includes('في شنو') ||
        msg.includes('شنو المشكلة')) {
        const responses = ['مالي بتكلم عادي', 'مالي؟', 'ما في شي'];
        return responses[Math.floor(Math.random() * responses.length)];
    }
    
    return null;
}

// ═══════════════════════════════════════════════════════════
// 🤖 دالة الذكاء الاصطناعي الرئيسية (محسّنة)
// ═══════════════════════════════════════════════════════════

async function getAIResponse(userMessage, config, userId = 'default', recentMessages = []) {
    if (!config.enabled || !config.apiKey) {
        return null;
    }

    try {
        // 1. فحص الردود السريعة أولاً
        const quickReply = getQuickResponse(userMessage);
        if (quickReply) {
            console.log(`⚡ رد سريع: ${quickReply}`);
            addToMemory(userId, userMessage, quickReply);
            return quickReply;
        }
        
        console.log(`🤖 طلب AI: ${userMessage.substring(0, 30)}...`);
        
        // 2. جلب الذاكرة القديمة
        const oldMemory = getMemory(userId);
        
        // 3. بناء المحادثة مع الذاكرة
        const messages = [
            {
                role: 'system',
                content: buildPersonalityPrompt()
            }
        ];
        
        // إضافة الذاكرة (من الملف + من المحادثة الحالية)
        const allMemory = [...oldMemory];
        
        // إضافة الرسائل الأخيرة من المحادثة الحالية
        if (recentMessages.length > 0) {
            const last5 = recentMessages.slice(-5);
            messages.push({
                role: 'system',
                content: `# آخر رسائل المحادثة:\n${last5.map((m, i) => 
                    `${i + 1}. ${m}`
                ).join('\n')}\n\nتذكر: افهم السياق والمعنى، وتكلم باللهجة السودانية بشكل طبيعي`
            });
        }
        
        // إضافة الذاكرة القديمة إن وُجدت
        if (allMemory.length > 0) {
            messages.push({
                role: 'system',
                content: `# محادثات سابقة (للسياق):\n${allMemory.map((m, i) => 
                    `${i + 1}. المستخدم: ${m.user}\n   مقداد: ${m.assistant}`
                ).join('\n')}`
            });
        }
        
        // الرسالة الحالية
        messages.push({
            role: 'user',
            content: userMessage
        });
        
        // 4. استدعاء API
        const response = await fetch('https://api.groq.com/openai/v1/chat/completions', {
            method: 'POST',
            headers: {
                'Authorization': `Bearer ${config.apiKey}`,
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                model: config.model,
                messages: messages,
                max_tokens: config.maxTokens,
                temperature: config.temperature
            })
        });

        if (!response.ok) {
            throw new Error(`Groq API error: ${response.status}`);
        }

        const data = await response.json();
        let reply = data.choices[0].message.content.trim();
        
        // 5. تنظيف الرد + تصحيح اللهجة
        reply = reply.replace(/\[.*?\]/g, '');
        reply = reply.replace(/\(.*?\)/g, '');
        
        // ⭐ تصحيح اللهجة تلقائياً
        reply = reply.replace(/\bما أعرف\b/g, 'ما عارف');
        reply = reply.replace(/\bآه\b/g, 'نعم');
        reply = reply.replace(/\bايه\b/g, 'شنو');
        reply = reply.replace(/\bحالياً\b/g, 'اسي');
        reply = reply.replace(/\bالحين\b/g, 'اسي');
        reply = reply.replace(/\bالآن\b/g, 'اسي');
        
        reply = reply.trim();
        
        console.log(`✅ رد AI: ${reply.substring(0, 30)}...`);
        
        // 6. حفظ في الذاكرة
        addToMemory(userId, userMessage, reply);
        
        return reply;

    } catch (error) {
        console.error('❌ خطأ AI:', error.message);
        return null;
    }
}

// ═══════════════════════════════════════════════════════════
// 🧹 دوال إدارة الذاكرة
// ═══════════════════════════════════════════════════════════

function clearMemory(userId) {
    if (userId) {
        conversationMemory.delete(userId);
    } else {
        conversationMemory.clear();
    }
}

function getMemoryStats() {
    return {
        total_users: conversationMemory.size,
        total_messages: Array.from(conversationMemory.values())
            .reduce((sum, mem) => sum + mem.length, 0)
    };
}

// ═══════════════════════════════════════════════════════════
// 📤 تصدير
// ═══════════════════════════════════════════════════════════

module.exports = {
    getAIResponse,
    clearMemory,
    getMemoryStats,
    KNOWLEDGE_BASE
};
