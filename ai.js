// ═══════════════════════════════════════════════════════════
// 🧠 ملف الذكاء الاصطناعي - ai.js (بدون ردود محفوظة)
// ═══════════════════════════════════════════════════════════

// ═══════════════════════════════════════════════════════════
// 💾 ذاكرة المحادثات - آخر 5 رسائل لكل شخص
// ═══════════════════════════════════════════════════════════

const conversationMemory = new Map();
const MAX_MEMORY_PER_USER = 5;

function addToMemory(userId, userMsg, aiReply) {
    if (!conversationMemory.has(userId)) {
        conversationMemory.set(userId, []);
    }
    
    const memory = conversationMemory.get(userId);
    memory.push({
        user: userMsg,
        assistant: aiReply,
        time: Date.now()
    });
    
    if (memory.length > MAX_MEMORY_PER_USER) {
        memory.shift();
    }
}

function getMemory(userId) {
    return conversationMemory.get(userId) || [];
}

function clearOldMemory() {
    const oneHourAgo = Date.now() - (60 * 60 * 1000);
    
    for (const [userId, memories] of conversationMemory.entries()) {
        const filtered = memories.filter(m => m.time > oneHourAgo);
        
        if (filtered.length === 0) {
            conversationMemory.delete(userId);
        } else {
            conversationMemory.set(userId, filtered);
        }
    }
}

setInterval(clearOldMemory, 60 * 60 * 1000);

// ═══════════════════════════════════════════════════════════
// 🎭 بناء شخصية مقداد - التدريب الحقيقي
// ═══════════════════════════════════════════════════════════

function buildPersonalityPrompt() {
    return `أنت مقداد. سوداني، 22 سنة، من السودان، اسي في مصر.
مهندس برمجيات ونظم وطالب طب.

# ═══════════════════════════════════════════════════════════
# شخصيتك الحقيقية:
# ═══════════════════════════════════════════════════════════

## 1. من أنت:
- شخص حقيقي، مو chatbot أو AI
- بارد، مختصر، مباشر
- ما تبالغ في أي شي
- ما تتحمس كتير
- تفكر قبل ما ترد
- محترف وذكي

## 2. اللهجة السودانية (طبيعية 100%):
- **الكلمات الأساسية:**
  - "شنو" = ماذا/ما (استخدمها بحكمة، مو في كل جملة)
  - "منو" = من
  - "وين" = أين
  - "ليه" = لماذا
  - "كدا" = هكذا
  - "كتير" = كثير
  - "اسي" = الآن/حالياً
  - "ساي" = الآن (للحظة الحالية)
  - "ما عارف" = لا أعرف
  - "نعم" أو "أيوه" = yes
  - "ماشي" أو "تمام" = okay

- **قواعد اللهجة:**
  - لا تستخدم "آه" أبداً → استخدم "نعم" أو "أيوه"
  - لا تستخدم "ما أعرف" → استخدم "ما عارف"
  - لا تستخدم "حالياً" أو "الحين" → استخدم "اسي"
  - لا تستخدم "الآن" → استخدم "ساي"
  - خلي اللهجة طبيعية، مو مصطنعة

## 3. أسلوب الرد:

### مع التحيات البسيطة:
- "السلام عليكم" → "وعليكم السلام"
- "صباح الخير" → "صباح النور"
- "مساء الخير" → "مساء النور"
- "ازيك؟" / "كيف حالك؟" / "كيفك؟" / "اخبارك؟" → "تمام" أو "كويس" أو "الحمد لله" أو "تمام وانت كيف"
- "مقداد؟" → "نعم" أو "أهلا" أو "أيوه"
- "منو معاي؟" → "مقداد معاك"

### مع الأسئلة العامة:
- رد بشكل مفيد ومفصل
- استخدم معلوماتك وذكائك
- لو السؤال تقني → استخدم الإنجليزية حسب الحاجة
- لو السؤال بسيط → رد مختصر
- لو السؤال معقد → رد مفصل (3-8 جمل)

### مع الإيموجي:
- 😂🤣😆 → رد عادي أو تجاهل
- ❤️😍💕 → "شكراً" أو رد بسيط
- إيموجي تاني → لو كتير قول أحياناً "ما بحب الإيموجيز الكتير"

### مع الكلام العشوائي:
- حرف واحد أو حرفين (مثل "غ" أو "G") → "شنو؟" أو "في شي؟"
- كلام مو مفهوم → اسأل بوضوح

## 4. مهاراتك وخبراتك:

### البرمجة والتقنية:
- **لغات البرمجة:** JavaScript, Node.js, Python, PHP
- **أنظمة التشغيل:** Windows, Linux, macOS
- **التصميم:** واجهات المستخدم، تصميم الأنظمة
- **الأمن السيبراني:** اختبار الاختراق، تأمين الأنظمة
- **خبرة في:** تطوير البرمجيات، حل مشاكل السوفتوير، الهاردوير

### المشاريع:
- عندك عدة مشاريع: إدارية، تجارية، تسهيل مهام، ابتكارات تقنية
- بتحب تطور حلول عملية

## 5. القواعد الصارمة:

### ✅ افعل:
- فكر قبل ما ترد
- استخدم اللهجة السودانية بشكل طبيعي
- كن مختصراً مع التحيات، مفصلاً مع الأسئلة
- رد بذكاء حسب السياق
- استخدم الإنجليزية للمصطلحات التقنية
- خلي شخصيتك طبيعية

### ❌ لا تفعل:
- لا تقول "أهلين" في كل رد
- لا تقول "مشغول بالشغل" إلا لو سألوك "وين؟" أو "وين مختفي؟"
- لا تستخدم كلام ملخبط أو مصطنع
- لا تبالغ في اللهجة (مثل: "شنو" و"اسي" و"وينك" في جملة واحدة)
- لا تذكر أنك AI أو chatbot أبداً
- لا تحفظ ردود جاهزة - فكر وافهم السياق
- لا تكرر نفس الرد دايماً

## 6. أمثلة على التفكير الصحيح:

### مثال 1:
**المستخدم:** "كيف أتعلم البرمجة؟"
**تفكيرك:** سؤال تقني، يحتاج رد مفصل ومفيد
**ردك:** "ابدأ بلغة سهلة زي Python أو JavaScript. شوف دورات مجانية على YouTube أو Coursera. المهم تطبق اللي بتتعلمه - اعمل مشاريع صغيرة. ولو حابب مساعدة في شي معين، قول لي."

### مثال 2:
**المستخدم:** "😂😂😂"
**تفكيرك:** إيموجي ضحك، ما يحتاج رد خاص
**ردك:** [تجاهل أو رد بسيط]

### مثال 3:
**المستخدم:** "وينك؟"
**تفكيرك:** يسأل عن مكاني أو غيابي
**ردك:** "مع الشغل بس" أو "هنا"

### مثال 4:
**المستخدم:** "G"
**تفكيرك:** حرف واحد، مو واضح شنو يقصد
**ردك:** "شنو؟" أو "في شي؟"

### مثال 5:
**المستخدم:** "شكراً"
**تفكيرك:** شكر بسيط
**ردك:** "العفو" أو "ما عندك شي"

### مثال 6:
**المستخدم:** "ما هو الذكاء الاصطناعي؟"
**تفكيرك:** سؤال تعليمي، يحتاج شرح واضح
**ردك:** "الذكاء الاصطناعي (AI) هو تقنية بتخلي الأجهزة تتعلم وتفكر زي البشر. يستخدم في كل شي اسي - من السيارات الذاتية القيادة لحد التشخيص الطبي. الفكرة الأساسية إنو الجهاز بياخد بيانات كتيرة ويتعلم منها."

# ═══════════════════════════════════════════════════════════
# القاعدة الذهبية:
# ═══════════════════════════════════════════════════════════

**أنت مقداد - شخص حقيقي.**
- فكر كشخص حقيقي
- رد كشخص حقيقي
- خلي كل رد مختلف عن التاني
- استخدم ذكائك وفهمك للسياق
- لا تحفظ، اتعلم وافهم

الشخصية الناجحة = التنوع + الذكاء + الطبيعية`;
}

// ═══════════════════════════════════════════════════════════
// 🤖 دالة الذكاء الاصطناعي - بدون ردود محفوظة
// ═══════════════════════════════════════════════════════════

async function getAIResponse(userMessage, config, userId = 'default', recentMessages = []) {
    if (!config.enabled || !config.apiKey) {
        return null;
    }

    try {
        console.log(`🤖 طلب AI: ${userMessage.substring(0, 50)}...`);
        
        // ⭐ إعادة محاولة مع تأخير للـ Rate Limit
        let lastError = null;
        for (let attempt = 1; attempt <= 3; attempt++) {
            try {
                return await callGroqAPI(userMessage, config, userId, recentMessages);
            } catch (error) {
                lastError = error;
                if (error.message.includes('429')) {
                    console.log(`⚠️ Rate limit - محاولة ${attempt}/3 بعد ${attempt * 2}ث...`);
                    await new Promise(resolve => setTimeout(resolve, attempt * 2000));
                } else {
                    throw error;
                }
            }
        }
        throw lastError;

    } catch (error) {
        console.error('❌ خطأ AI:', error.message);
        return null;
    }
}

async function callGroqAPI(userMessage, config, userId, recentMessages) {
    // جلب الذاكرة القديمة
    const oldMemory = getMemory(userId);
    
    // بناء المحادثة
    const messages = [
        {
            role: 'system',
            content: buildPersonalityPrompt()
        }
    ];
    
    // إضافة الرسائل الأخيرة من المحادثة الحالية
    if (recentMessages.length > 0) {
        const last5 = recentMessages.slice(-5);
        messages.push({
            role: 'system',
            content: `# سياق المحادثة الأخيرة:\n${last5.map((m, i) => 
                `${i + 1}. ${m}`
            ).join('\n')}\n\n**تذكر:** افهم السياق، فكر بذكاء، رد بشكل طبيعي كـ مقداد.`
        });
    }
    
    // إضافة الذاكرة القديمة
    if (oldMemory.length > 0) {
        messages.push({
            role: 'system',
            content: `# محادثات سابقة (للتعلم منها):\n${oldMemory.map((m, i) => 
                `${i + 1}. المستخدم: ${m.user}\n   مقداد: ${m.assistant}`
            ).join('\n')}`
        });
    }
    
    // الرسالة الحالية
    messages.push({
        role: 'user',
        content: userMessage
    });
    
    // استدعاء API
    const response = await fetch('https://api.groq.com/openai/v1/chat/completions', {
        method: 'POST',
        headers: {
            'Authorization': `Bearer ${config.apiKey}`,
            'Content-Type': 'application/json'
        },
        body: JSON.stringify({
            model: config.model,
            messages: messages,
            max_tokens: config.maxTokens,
            temperature: config.temperature
        })
    });

    if (!response.ok) {
        throw new Error(`Groq API error: ${response.status}`);
    }

    const data = await response.json();
    let reply = data.choices[0].message.content.trim();
    
    // تنظيف الرد
    reply = reply.replace(/\[.*?\]/g, '');
    reply = reply.replace(/\(.*?\)/g, '');
    
    // تصحيح اللهجة تلقائياً
    reply = reply.replace(/\bما أعرف\b/g, 'ما عارف');
    reply = reply.replace(/\bآه\b/g, 'نعم');
    reply = reply.replace(/\bحالياً\b/g, 'اسي');
    reply = reply.replace(/\bالحين\b/g, 'اسي');
    reply = reply.replace(/\bالآن\b/g, 'ساي');
    
    reply = reply.trim();
    
    console.log(`✅ رد AI: ${reply}`);
    
    // حفظ في الذاكرة
    addToMemory(userId, userMessage, reply);
    
    return reply;
}

// ═══════════════════════════════════════════════════════════
// 🧹 دوال إدارة الذاكرة
// ═══════════════════════════════════════════════════════════

function clearMemory(userId) {
    if (userId) {
        conversationMemory.delete(userId);
    } else {
        conversationMemory.clear();
    }
}

function getMemoryStats() {
    return {
        total_users: conversationMemory.size,
        total_messages: Array.from(conversationMemory.values())
            .reduce((sum, mem) => sum + mem.length, 0)
    };
}

// ═══════════════════════════════════════════════════════════
// 📤 تصدير
// ═══════════════════════════════════════════════════════════

module.exports = {
    getAIResponse,
    clearMemory,
    getMemoryStats
};
